// Project Type
export interface ProjectType {
    id: number;
  date_role: string;
  description: string;
  role: string;
}

// Research Type History
export interface ResearchType {
  resTitle: string;
  resDescription: string;
  resDate: string;
  resYear: string;
  resLocation: string;
  
}